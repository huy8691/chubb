# URL gốc dùng cho link chia sẻ, mã QR, đường dẫn danh thiếp công khai.
# Đặt bằng URL deploy thật (vd trên Vercel) để QR/link mở đúng trang.
# Bỏ trống: client tự lấy window.location.origin (origin trang đang mở); SSR/fallback dùng hằng trong routes.ts.
NEXT_PUBLIC_SITE_ORIGIN=https://your-deploy-url.vercel.app
